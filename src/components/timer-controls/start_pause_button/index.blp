using Gtk 4.0;
using Adw 1;

template $StartPause : Gtk.Button {
  styles ["suggested-action", "circular", "large-button"]
  halign: center;
  valign: center;
  icon-name: "media-playback-start-symbolic";
  tooltip-text: _("Init timer");
  clicked => $_start_pause_timer();
}